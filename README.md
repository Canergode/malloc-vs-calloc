# Malloc vs Calloc

This project demonstrates the difference between malloc and calloc for dynamic memory allocation. malloc allocates memory without initialization while calloc initializes all bytes to zero.

## What I Learned
- The difference between malloc and calloc
- How calloc initializes memory to zero unlike malloc
- How to check both allocations with a single NULL check
- How to properly free multiple dynamically allocated arrays
